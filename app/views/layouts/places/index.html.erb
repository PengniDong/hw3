<h1>Places I've Been</h1>

<ul>
  <% @places.each do |place| %>
    <li>
      <!-- Link to that place's show page -->
      <%= link_to place.name, "/places/#{place.id}" %>
    </li>
  <% end %>
</ul>

<p>
  <%= link_to "Add New Place", "/places/new" %>
</p>